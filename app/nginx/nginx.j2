server {
 listen 80;
       listen [::]:80;

       server_name mspr_test;

       root /var/www/mspr;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}

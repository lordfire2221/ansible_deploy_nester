server {
 listen 80;
       listen [::]:80;

       server_name nester_servers;

       root /var/www/mspr/final_mspr/frontend;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}
